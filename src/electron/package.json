{
  "name": "sbx-helper",
  "version": "1.0.0",
  "private": true,
  "description": "Builds sbx run commands from a folder picker — browser mode or a native desktop app.",
  "main": "electron-main.mjs",
  "scripts": {
    "postinstall": "patch-package",
    "start": "electron .",
    "test": "node --test",
    "package": "electron-forge package",
    "make": "electron-forge make"
  },
  "devDependencies": {
    "@electron-forge/cli": "^7.11.2",
    "@electron-forge/maker-zip": "^7.11.2",
    "electron": "^44.2.0",
    "patch-package": "^8.0.1"
  }
}
